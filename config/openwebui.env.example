# ============================================================================
# OpenWebUI Backend Environment Configuration
# ============================================================================
# Copy this file to openwebui.env and update with your actual values
# ============================================================================

# ----------------------------------------------------------------------------
# Environment
# ----------------------------------------------------------------------------
# Environment mode: development, production
ENV=production

# Port for the backend server (internal container port)
PORT=8080

# ----------------------------------------------------------------------------
# Ollama Configuration
# ----------------------------------------------------------------------------
# Use Ollama in Docker or external
USE_OLLAMA_DOCKER=false

# Ollama API endpoint
# Use host.docker.internal to access Ollama running on host machine
OLLAMA_BASE_URL=http://host.docker.internal:11434

# ----------------------------------------------------------------------------
# OpenAI Configuration (Optional)
# ----------------------------------------------------------------------------
# Add your OpenAI API key if using OpenAI models
OPENAI_API_KEY=your_openai_api_key_here

# ----------------------------------------------------------------------------
# Security Configuration
# ----------------------------------------------------------------------------
# Secret key for session management (generate a random string)
WEBUI_SECRET_KEY=change_this_to_a_random_secret_key

# ----------------------------------------------------------------------------
# Analytics & Telemetry
# ----------------------------------------------------------------------------
# Disable analytics and telemetry
SCARF_NO_ANALYTICS=true
DO_NOT_TRACK=true
ANONYMIZED_TELEMETRY=false

# ----------------------------------------------------------------------------
# Audio Configuration
# ----------------------------------------------------------------------------
# Whisper model for speech-to-text
# Options: tiny, base, small, medium, large
WHISPER_MODEL=base

# ----------------------------------------------------------------------------
# RAG (Retrieval Augmented Generation) Configuration
# ----------------------------------------------------------------------------
# Embedding engine: ollama, openai, sentence-transformers
RAG_EMBEDDING_ENGINE=ollama

# Embedding model to use
RAG_EMBEDDING_MODEL=nomic-embed-text

# ----------------------------------------------------------------------------
# Vector Database Configuration
# ----------------------------------------------------------------------------
# Vector database type: qdrant, chroma, milvus
VECTOR_DB=qdrant

# Qdrant configuration
QDRANT_HOST=nostack-qdrant
QDRANT_PORT=6333
QDRANT_URI=http://nostack-qdrant:6333

# ----------------------------------------------------------------------------
# Zep Memory Configuration
# ----------------------------------------------------------------------------
# Zep API endpoint for conversational memory
ZEP_API_URL=http://nostack-zep:8002

# Zep API key (must match ZEP_API_KEY in main .env)
ZEP_API_KEY=change_this_zep_api_key

# ----------------------------------------------------------------------------
# Optional: Additional LLM Providers
# ----------------------------------------------------------------------------
# Anthropic (Claude)
# ANTHROPIC_API_KEY=your_anthropic_api_key

# Google (Gemini)
# GOOGLE_API_KEY=your_google_api_key

# Cohere
# COHERE_API_KEY=your_cohere_api_key

# ----------------------------------------------------------------------------
# Optional: Advanced Configuration
# ----------------------------------------------------------------------------
# Enable/disable features
# ENABLE_SIGNUP=true
# ENABLE_LOGIN_FORM=true
# DEFAULT_USER_ROLE=user

# File upload limits
# UPLOAD_SIZE_LIMIT_MB=100

# Database URL (if not using default)
# DATABASE_URL=postgresql://user:password@host:port/database

# ============================================================================
# SECURITY NOTES
# ============================================================================
# 1. Change WEBUI_SECRET_KEY to a strong random value
# 2. Update ZEP_API_KEY to match your Zep configuration
# 3. Never commit API keys to version control
# 4. Use environment-specific configurations for dev/staging/prod
# 5. Regularly rotate API keys and secrets
# ============================================================================
