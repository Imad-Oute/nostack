# Neo4j

This directory is a placeholder for Neo4j plugins or custom configurations.

## How to use
1. Place any custom plugins or extension code here.
2. Map this directory in `docker-compose.yml` if needed.
